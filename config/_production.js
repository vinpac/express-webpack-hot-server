// these will override the base config
module.exports = function(config) {
  return {
    // Define or replace the configuration for Production
    server_open_browser: false,
    server_preserve_log: true
  }
}
